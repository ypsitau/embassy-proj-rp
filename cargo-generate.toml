[template]
cargo_generate_version = ">=0.18.0"

[placeholders.template]
type = "string"
prompt = "Choose a template"
choices = ["GPIO In/Out", "USB device CDC", "Wi-Fi HTTP Client"]
default = "GPIO In/Out"
